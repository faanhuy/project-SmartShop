namespace SmartShop.Domain.Enums;

public enum SizeType
{
    DrinkSize = 0,   // Đồ uống (Small, Medium, Large, Extra Large)
    FoodPortion = 1, // Khẩu phần ăn (Small, Regular, Large)
    MealSize = 2,    // Suất ăn/Combo (1 người, 2 người, Gia đình)
    Custom = 3       // Tùy chỉnh
}
